<resources>
    <!-- Base application theme. -->
    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/bg_splash</item>
        <item name="colorPrimaryDark">@color/bg_splash2</item>
        <item name="colorAccent">@color/bg_splash3</item>
    </style>

    <style name="Theme.App.Base" parent="Theme.AppCompat.Light.NoActionBar">
        <item name="colorControlNormal">@color/bg_splash</item>
        <item name="colorControlActivated">@color/bg_splash2</item>
        <item name="colorControlHighlight">@color/bg_splash3</item>
    </style>

<!--    <style name="SplashTheme" parent="Theme.AppCompat.Light.NoActionBar">-->
<!--        <item name="android:windowBackground">@drawable/splash_screen</item>-->
<!--    </style>-->

    <!-- No Action Bar theme. -->
    <style name="NoActionBar" parent="Theme.AppCompat.Light.NoActionBar">
        <!-- Customize your theme here. -->
        <item name="colorPrimary">@color/bg_splash</item>
        <item name="colorPrimaryDark">@color/bg_splash2</item>
        <item name="colorAccent">@color/bg_splash3</item>
    </style>

    <!-- No Action Bar theme. -->
    <style name="MyLightActionBar" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">@color/bg_splash</item>
        <item name="colorPrimaryDark">@color/bg_splash2</item>
        <item name="colorAccent">@color/bg_splash3</item>
    </style>

</resources>